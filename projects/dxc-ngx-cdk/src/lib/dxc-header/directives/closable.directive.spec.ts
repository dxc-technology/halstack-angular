import { ClosableDirective } from './closable.directive';

describe('ClosableDirective', () => {
  it('should create an instance', () => {
    const directive = new ClosableDirective();
    expect(directive).toBeTruthy();
  });
});
