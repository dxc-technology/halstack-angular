/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DxcActivationcodeService } from './dxc-activationcode.service';

describe('Service: DxcActivationcode', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DxcActivationcodeService]
    });
  });

  it('should ...', inject([DxcActivationcodeService], (service: DxcActivationcodeService) => {
    expect(service).toBeTruthy();
  }));
});
