import { MandatoryDirective } from './mandatory.directive';

describe('MandatoryDirective', () => {
  it('should create an instance', () => {
    const directive = new MandatoryDirective();
    expect(directive).toBeTruthy();
  });
});
