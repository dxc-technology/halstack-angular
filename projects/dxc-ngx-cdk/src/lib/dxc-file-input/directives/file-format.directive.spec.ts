import { FileFormatDirective } from './file-format.directive';

describe('FileFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new FileFormatDirective();
    expect(directive).toBeTruthy();
  });
});
